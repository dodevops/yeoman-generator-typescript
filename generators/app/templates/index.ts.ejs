/**
 * @module <%= name %>
 */
/**
 */

// import needed modules

import * as loglevel from 'loglevel'<% if (promise) { %>
import Bluebird = require('bluebird')<% } %>

/**
 * <%= name %> - <%= description %>
 */
export class ExampleClass {

  /**
   * Use a logger instance to log, what you're doing
   */
  private _log: loglevel.Logger = null

  constructor () {
    this._log = loglevel.getLogger('<%= name %>:ExampleClass')
  }
<% if (promise) { %>
  /**
   * Be nice.
   * @param {string} name Who are you?
   * @return {Bluebird<string>} My greets to you
   */
  public helloWorld (name: string): Bluebird<string> {
    if (name === '') {
      return Bluebird.reject(new Error('Found nobody to greet'))
    }
    return Bluebird.resolve(`Hello ${name}`)
  }
<% } else { %>
  /**
   * Be nice.
   * @param {string} name Who are you?
   * @return {string} My greets to you
   */
  public helloWorld (name: string): string {
    if (name === '') {
      throw new Error('Found nobody to greet')
    }
    return `Hello ${name}`
  }
<% } %>
}
